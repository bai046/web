// 脚本语言，用前端检查错误
function orange(){
    var array = [1,2,6,0,6,3,7,54,32]
    for (let index = 0; index < array.length; index++) {
        const element = array[index];
        console.log(element);
        
    }
}
orange();
